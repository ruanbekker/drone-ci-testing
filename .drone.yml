pipeline:
  docker:
    image: docker
    environment:
      MYUSERNAME:
        from_secret: myusername
    commands:
      - echo $${MYUSERNAME}
      - echo "run export command"
      - export
      - echo $MYUSERNAME > /tmp/file
      - cat /tmp/file
